{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the desktop shell needs. Custom commands defined in lib.rs are not permission-gated; plugin commands are, which is why the dialog needs an entry here.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
