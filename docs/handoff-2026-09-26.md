# Handoff, 26 September 2026: D135 to D138 fixed as one; D134 folds into step 6

Branch `identity/signed-authorship`, draft PR #5. The rulings in
`docs/handoff-2026-09-24.md` and `docs/handoff-2026-09-25.md` still bind; the
standing rules are as there, with **one step per session** and **one handoff
per date, always the latest**.

## Where things stand

- **Pushed:** through `5fc27bc` (the fix), and this handoff on top.
- **CI read with `scripts/ci-verdict.mjs`:**
  - `f459d5e` (last handoff), run `36215858300`: success. Chromium+node 1274,
    WebKit 403 + 355, Firefox (reading) 751 with 1 flaky
    (`mailbox-link-e2e:1588`).
  - `ebb7599` (the fold), run `36237812379`: **failure, on `checks` only**:
    `tests/impact-map.json is stale`. Every browser job passed (chromium+node
    1282, WebKit 403 + 355, Firefox 751 with 1 flaky,
    `returning-document:381`, D32). The map was written while the untracked
    probe file was still on disk, so it counted a spec the pushed tree does
    not hold. The next commit's map is right; the fold commit stays red on
    `checks` in history.
  - `5fc27bc` (the fix), run `36239527779`: success. Chromium+node 1284,
    WebKit 403 + 355, Firefox 752, 0 failed, 0 flaky, `checks` pass. Three
    tests added over the fold and the count rose by two: `d22-reopen:209`
    skipped itself (30 to 31 chromium skips), found by diffing the skip lines.
  - Local, on the committed tree `5fc27bc`: chromium+node 1287 passed, 0
    failed.
- **Not read:** this handoff's own run.

## What this session did

Rulings given at the start, recorded in each entry's status line: D135 to D138
are one fix; a writer decides the head the way admission does, through the
admitted view for its own session and seat, never the stored flag; the probe
file folds into `seat-attacks.spec.ts`. D134 folds into step 6's format bump:
the entity key becomes (session, entity) there, not before.

- **`ebb7599`, the fold:** the third cold review's reproduction as a "cold
  review 3" section of `tests/seat-attacks.spec.ts`: six `test.fail` naming
  D135 (twice, lc 1 and 1000000), D136, D137 (twice) and D138, plus the
  convergence test, which passes. Each was run without its `test.fail` first,
  and each failed on the attack's own assertion, not on setup. The untracked
  probe is deleted.
- **`5fc27bc`, the fix:** `writeTargetOf` (`src/replicated-rows.ts`) is the one
  reading of what a write versions, and `headsOf`, `changeEntity`,
  `deleteEntity`, reseat and the bootloader's write gates (`sessionOfEntity`)
  all take their heads and session from it:
  - a plain table: `t_heads`, which is the flag there;
  - an admission-filtered session table: `t_heads` in a session this copy is
    a member of **or waits in**, in a seated table only its own rows, plus its
    own rows in `t_pending`;
  - a roster table: its own versions in the session, as `_dai_open_seat`
    counts the creator's.

  No partition refuses the write `ROW_REJECTED`. Two partitions (D134's id
  reuse across partitions this copy writes in) refuse it too, rather than
  guess. `t_pending` works out supersession within its partition, by an
  admitted or waiting row (D138), and `_dai_open_seat`'s retire check requires
  the same session (D136). The six `test.fail` flipped as "expected to fail,
  but passed" before they came off. Three tests added: a waiting joiner
  versions his own waiting move; a waiting joiner's rename of the creator's
  game waits, then stands once he is confirmed; an id in two of this copy's
  sessions is refused (red on the old writer). The spec's rule 5 has one
  sentence on the writers.

## What the first full run found

The writer's first form admitted only members. The node gate passed, but the
full chromium run failed `push-e2e:523` and `:972`: the page said "That did
not save: This copy holds no version of that games row it may write". A
tic-tac-toe joiner renames the game while his own copy still waits on the
creator's confirmation. The old writer allowed that, and the row waits in
`_pending` until he is confirmed; the rules say a waiting copy's rows are
pending, not refused. So "a session this copy writes in" became "member or
waiting". The node test that holds it was shown red first on the member-only
form, with the same message. Two `replicated-converge` tests wrote into
sessions with no roster, rows no copy admits; they now write in a session
their author created.

## Order remaining

1. **A cold review of this fix is owed**, bounded as before, at its seams: the
   writer's partition against `_heads`, `_pending` and the roster views, a
   waiting copy's writes, and reseat. Step 5's seat model closes when it is
   read.
2. D127 (the arriving link's key outliving the arrival; high, not yet run).
3. Step 6: the format bump carries D134 (entity key (session, entity)), the
   unsigned-row refusal for every table, and the whole-file load path (D133's
   note).
4. The D124 route audit (its own session).
5. D32's bounded chase of the three rungs.

Still filed and not ordered: D139 (a WebKit retry; not seen on these two
runs), D120, D121, D123 items 2 and 3, D124, D128 to D130, the four small
items under D117.

## Traps met this session

- **An untracked file is in the impact map.** `scripts/impact-map.mjs --write`
  scans the working tree, so a probe spec on disk and not in the commit made
  the pushed map stale and `checks` red. Write the map with the tree exactly
  as it will be pushed: delete or move any untracked spec first.
- **A node gate cannot see what only a page does.** The member-only writer
  passed every node test; a joiner renaming while waiting happens only in the
  e2e flow. Run the full chromium project before calling a writer change done.
- **A comment moved an anchor.** `SESSION-ROW-CARRIES-SESSION` anchors on
  comment text in `src/replicated-rows.ts`; rewording the comment failed
  `rules.spec.ts:65`. The rule still held, so the anchor moved, not the rule.
- **Tests that read HEAD fail before the commit.** `staged-runtime:61` and
  `host-retention:49` compare against committed files: commit locally, then
  run the full suite on that tree before pushing.
- **An intermediate build leaves a host shell.** Every `npm run build` after a
  source edit keeps a new `apps/runner/public/hosts/<id>`; drop the ones from
  builds that never shipped, from the directory and `index.json`.
- **The failure-reason check.** A `test.fail` counts as passed, so its red is
  read by running a copy with the `test.fail` lines stripped
  (`grep -v 'test.fail(true, "D13'`), then deleting the copy.
