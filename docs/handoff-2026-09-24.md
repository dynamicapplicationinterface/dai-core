# Handoff, 24 September 2026: identity sitting, mid step 5

Branch `identity/signed-authorship`, draft PR #5. Read `docs/identity.md`
(the spec), `docs/format.md`, and the identity entries in `src/rules.ts`.
Standing rules: reading code is the hypothesis, running it is the test; a red
must fail on the assertion it claims; full chromium + node locally on the exact
tree before every push, with `--output` to a temp dir (a second Playwright run in
the same checkout wipes `test-results/`); regenerate every product and commit it
whole; one cold review per step; US English; no commercial or plan language in
the repo; report each commit and each CI verdict with run ids.

## Where things stand

- Steps 1–4 are closed. Step 4's review fixes, the lane race (D113) and the
  docs sweep are pushed.
- **Step 5 is built and pushed but does not close D80.** Pushed head `7d5a3f0`;
  CI run `36059363617` was green except Firefox `returning-document:511`, which
  is D32 (see below). The cold review then found the seat model broken:
  - **Critical, reproduced.** A joiner can take the creator's seat with a
    backdated or equal-clock `_dai_binding`. Merged into Ada's copy, Bo's White
    move is admitted, Ada's e4 drops silently, and `refusedBatches` is `[]`.
  - **Critical, reproduced.** A joiner becomes the creator with a backdated
    `_dai_seat` row (`_dai_creator` = first seat row by clock, any author),
    even with every row signed.
  - **High.** Any member can write a new version of the creator's seat entity
    and void her later moves. An honest forwarded invite erases an honest
    player's moves about half the time (same clock, random author ids decide).
    The contested e2e's `if (bHolds)` branches hide that.
  - The reviewer's scripts: `%TEMP%\review5s\attack.spec.ts` (run with
    `npx --prefix <repo> playwright test attack.spec.ts -c pw.config.mjs
    --reporter=list`; the tests only log, so read the output, not the pass count).
- Local commit not pushed: `2eb86af` (D113 corrected: it did not clear :511;
  D32 bumped).
- **Uncommitted in the tree:** `tests/cross-entity-parent.spec.ts`, finding 5's
  red test, red on its three claims (a row naming another entity's row as parent
  buries it: plain table, parent arriving later, and a seated session table),
  and green on its fourth (within one entity an edit still supersedes).

## The rulings to build (given 24 September, verbatim in substance)

1. **Seat model, all three:**
   - The session id commits to its creator:
     `SHA-256(creator author id ‖ nonce)`, first 16 bytes, with the nonce on the
     creator's first seat row.
   - The creator's seat is the creator's by definition.
   - The open seat is held by whoever the creator confirms, in a row only the
     creator writes. The kit writes it automatically on the creator's copy when
     it sees exactly one binding, and shows a contest when it sees two.
   - No clock decides anything.
   - The first-verified-signer rule is withdrawn: record in `rules.ts` that it
     was ruled and replaced on 24 September, with the attack that killed it.
   - D114 closes into this.
   - The contested e2e drops its `if (bHolds)` branches and asserts the one
     outcome.
2. **Finding 5:** fix now, own red test (written, above), own commit. A parent
   outside the row's own entity hides nothing. It predates step 5. The places
   that decide supersession:
   - `applyRow` (marks parents; superseded-on-arrival);
   - the admission-filtered heads view (`headsView` in `src/replicated.ts`);
   - the `__superseded_monotonic` trigger's naming check;
   - the merge's `displace` resettle;
   - the Python and Rust readers. The Rust reader recomputes over the whole
     table; add a merge vector with a cross-entity parent so the readers have to
     agree.
3. **Finding 6:** pending, not refused. A move waiting on confirmation is
   neither admitted nor in `refusedBatches`. No new report field; a kit read
   `pendingSeat(session)` is enough. `SEAT_NOT_HELD` is reported only when the
   seat is held by someone else at the row's clock, or none is named.
4. **Finding 7:** keep the partial-loss trigger. `docs/identity.md`, under
   Loss, says a full wipe cannot be detected from what is left on the device,
   and that a recovery phrase is what would change that; not built.
5. **Finding 10:** file with the numbers (5.5 s at 1,000 moves, +55% from the
   seat check; superlinear before it). **Finding 8** files beside it, with the
   spec's sentence: the scan is a courtesy; admission is the enforcement.
- **D32:** done in `2eb86af`. Not chased this sitting.

**Order:**
1. Finding 5 (red is written; fix, readers, vector, products, commit).
2. The reviewer's two attack scripts become named tests, red on the current
   code, before the model changes.
3. The model rebuild.
4. D80's green re-earned on the new model, not carried.
5. Gate, push, CI per engine.
6. A second cold review over step 5, with the reviewer's scripts in its range.
7. Then step 6 (legacy both directions, D108, `BATCH_UNSIGNED`), then step 7
   (rebuild examples: tic-tac-toe and request are the `check-seats` exceptions
   and the reason `seat-table-write` is a lint warning, not breaking).

## What step 5 built (still true, parts to be replaced)

- `-- dai:replicated seat=<column>` marker (`markerClauses`); `_dai_seat_rules`.
- Views `_dai_creator`, `_dai_holder`, `_dai_member`, and `<table>_unseated`.
  `heldThen` in `headsView` holds "at the row's clock". The creator/holder
  rules are what the rulings replace.
- `mergeFrom` reports `SEAT_NOT_HELD` (the 70th code) from `_unseated`, for
  rows added in that merge.
- Runtime: `replicated.author()`, `writable()`, `seatGate`, and `creatorIs`
  (reads `_dai_creator`).
- Kit (`src/kit.ts`): `newSession({solo})`, `claimSeat`, `reseat`, `mySeat`,
  `amCreator`, `seats` (with `values` history), `seatBytes`, `whenWritable`,
  `onNewPlayer`, `author`.
- Loss sentence: host `newAuthor` (`mintedThisPage` and library `wrote`, not
  written this page); frame `window.dai.newPlayer` plus `dai:new-player`
  (`FRAME_PUBLIC.NEW_PLAYER`).
- Chess fixture: moves and game_events carry `seat`; side read from the seat;
  boot writes via `whenWritable`.
- `scripts/check-seats.mjs` plus `src/seat-check.ts`, and lint
  `seat-table-write` (warning).
- `rules.ts`: IDENTITY-SEAT-ADMITS, IDENTITY-FIRST-SIGNER (to be withdrawn),
  IDENTITY-KIT-SEATS, IDENTITY-LOSS-SENTENCE, IDENTITY-BOOT-WRITES. Model-file
  budget 135,450.
- The D80 e2e forges by raw SQL, naming Ada's seat, with no binding. The
  review's stronger forgery adds a backdated binding first; make the e2e do
  that on the new model.

## Traps met this sitting

- Shell escaping: node `-e` with backticks, `\s` or `${` breaks. Heredocs are
  blocked by a hook. Write edit scripts to the scratchpad with the Write tool,
  or use Edit.
- `rules.spec.ts` runs in the browser projects, not node (D110).
- A mutation edit must be verified to have applied (`grep -c`) before its
  result means anything.
