# Handoff, 25 September 2026: identity step 5, step 3 committed; D117 next

Branch `identity/signed-authorship`, draft PR #5. The rulings in
`docs/handoff-2026-09-24.md` still bind; the standing rules are as there, with
**one step per session** (a step ends when its gate is green and its cold
review is read).

## Where things stand

- **Pushed:** up to `7a35fb5`, as before.
- **Local, not pushed:** `873fadd` (finding 5), `2e3d227` (the attacks red),
  `916004c` (handoff), and **`817de58`, step 3 whole: the seat model.** Its
  message lists what was built. The tree is clean.
- **Commit tier on the step 3 tree** (it reached everything, so it ran as the
  push tier): 1256 passed, 27 skipped, 0 failed; count gate chromium 757,
  node 498. `host-retention` and `staged-runtime` rerun on `817de58`: 24 passed.
- **Known red, WebKit only** (outside the chromium + node tier):
  `invite-identity` "the recipient is not the sender, on an iPhone" and
  "test 1". Both fail at "the copy runs a mailbox session". That is D117.

## Rulings given this session (24 September)

- **First ask wins over the mailbox.** The creator's copy reads one batch per
  merge, so it seats the first ask it reads; a contest needs two asks in one
  merge. A later asker is told the place is taken (the fixture's
  "your seat here was taken").
- **Reseat is contest-only, as built.** A confirmed seat is held for good. A
  creator-side eject is filed as **D118** with its open question (what happens
  to the ejected player's admitted moves), not built. The answer to "the wrong
  person opened a forwarded invite first" is a new game, not forwarding the link.
- **A joiner waits to be seated.** `invite-identity` test 6 asserts the author
  id and the waiting state, then the name prompt once the creator's copy has
  seated it. Tests 1 and the iPhone recipient go through the same helper
  (`waitsThenIsAsked`). The spec now sets the relay the way a deploy does,
  through the `dai-relay` meta on every load, and asserts that each copy runs
  a mailbox (`mailboxRuns`), because `__runner.useRelay` after the fact cannot
  reach the load before an iOS relaunch.
- **D117 is its own step, before D80.** It is a launch/relaunch path change, and
  those get a cold review of their own before anything downstream leans on them.

## Order remaining

1. **D117** (`docs/backlog.md`): a named red for the lost key (a device that
   does not hold the app opens an invite on an iPhone; after the relaunch the
   library holds the game's key), then the fix, then a cold review of that
   change alone. Where it starts: the arriving key is filed at arrival only when
   `heldHere` (`apps/runner/src/main.ts`, "The game's key is filed here"),
   otherwise in `startMailboxIfPossible`, which the relaunch outruns. The two
   WebKit reds above must go green with it; run them on WebKit, not only
   Chromium.
2. D80 green re-earned (the handoff of 25 September, step 4, unchanged).
3. Full chromium + node gate on the exact tree, push, CI per engine
   (`scripts/ci-verdict.mjs`).
4. Second cold review over step 5 with `%TEMP%\review5s\attack.spec.ts` in range.
5. Step 6.

## Traps met this session

- **The practice board is an ask.** Counting "sessions this copy bound" includes
  its own solo practice board, where it seats itself. Count only sessions it did
  not create (`!kit.amCreator(s)`).
- **A relay injected after load misses the iOS relaunch.** `useRelay` sets the
  relay on the page that exists; the load that opened the link is already gone.
  Set it through the meta, from an init script, at `readyState` "interactive".
- **The creator has no mailbox until it shares.** A document opened from a file
  has no key; assert the creator's mailbox after the link exists, not before.
