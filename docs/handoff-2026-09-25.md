# Handoff, 25 September 2026: D131 to D133 fixed; the third cold review, at their seams, finds four more

Branch `identity/signed-authorship`, draft PR #5. The rulings in
`docs/handoff-2026-09-24.md` still bind; the standing rules are as there, with
**one step per session** (a step ends when its gate is green and its cold
review is read). One handoff per date, always the latest: this replaces the
second-cold-review handoff of the same date (`9e71757`), in git history.

## Where things stand

- **Pushed:** through `4660092` (D132), and this handoff on top.
- **CI read with `scripts/ci-verdict.mjs`:**
  - `9e71757` (last handoff), run `36211054715`: success. Chromium+node 1261,
    WebKit 403 + 355, 0 failed; Firefox (reading) 750, 2 flaky
    (`returning-document:381`, `:511`, D32).
  - `1f8e8cb` (the fold), run `36212419579`: success. Chromium+node 1273,
    WebKit 403 + 355, Firefox 752, 0 failed, 0 flaky.
  - `734a4c3` (D133), run `36213297105`: success. Chromium+node 1273, WebKit
    403 + 354 with 1 flaky (`sealed-leave:25`, D139), Firefox 751 with 1 flaky
    (`returning-document:511`).
  - `0114b53` (D131), run `36214061757`: success. Chromium+node 1274, WebKit
    403 + 354 with 1 flaky (`sealed-leave:25` again, D139), Firefox 751 with 1
    flaky (`container:914`).
  - `4660092` (D132), run `36214659478`: success. Chromium+node 1274, WebKit
    403 + 355, Firefox 752, 0 failed, 0 flaky. It added a test and passed the
    same count as `0114b53` because `d22-reopen:209` skipped itself (31 to 32
    skipped), found by diffing the skip lines.
- **Not read:** this handoff's own run.

## What this session did

The rulings were given at the start of the session and are recorded in each entry's status line:
D133 first (unsigned seat rows refused now), then D131 (a seat is
(session, seat), an entity belongs to one session), then D132 (a row
supersedes only rows of its own seat and session). Each was shown red on its
own `test.fail` before its fix, and each `test.fail` came off as its fix
landed, flipping as "expected to fail, but passed" first.

- **`1f8e8cb`**: the second cold review's reproduction folded into
  `tests/seat-attacks.spec.ts`, its five holes as `test.fail`, each naming the
  entry that flips it.
- **`734a4c3`, D133:** a merge refuses an unsigned row in `_dai_seat`,
  `_dai_binding`, `_dai_confirm` as `BATCH_UNSIGNED`, under the author it
  names, with no exemption for this copy's own id (the forgery is a row under
  the creator's id arriving at her copy); a row already held at that id in that
  table takes the ordinary duplicate-or-reject path. Tests that carried
  unsigned seat rows across a merge now sign with real keys
  (`tests/signed-people.ts`): `roster`, two in `seat-admission`, one in
  `replicated-converge`.
- **`0114b53`, D131:** in an admission-filtered session table a row supersedes
  only rows of its own session, `_current` picks one head per (session,
  entity), and a row naming a parent of another session is stored, never
  admitted, and reported `ENTITY_OTHER_SESSION` (new code; a view over the row
  set, so every copy answers the same whatever arrived first). The kit's
  `seats(session).values` reads only the creator's own seat rows in that
  session; chess's `moves` and `events` read the game's own session. The
  attack tests now read Ada's game through the chess fixture's own `Store`,
  not a copy of its SQL, and were shown red on that read first.
  `IDENTITY-SEAT-ADMITS` says the pair and why.
- **`4660092`, D132:** in a seated table a row naming a parent of another seat
  of its session is stored, never admitted, and reported `SEAT_NOT_HELD`;
  supersession and `_current` are per (session, seat, entity). A counter-case
  holds that the creator's own version of her move still replaces it.
- **Filed:** D134 (an entity id reused in another session is a second entity;
  an app reading by id alone sees two; the order-free fix is an entity id that
  commits to its session, a format change).

**The third cold review**, bounded (six findings or an hour), a fresh agent
told all three fixes and asked about the seams between them. Every finding was
rerun here before being relayed. **Nothing high.** Convergence held (three
copies, any merge order, one `_current`), and the reports agreed with the
admission views in every run. All four findings are one seam: the honest
writers (`headsOf`, `changeEntity`, `deleteEntity`) still version an entity by
its id alone, across sessions and seats, while admission now partitions it.

- **D135** (medium): a stranger's parentless row reusing a game's id freezes
  the creator's rename, and the merge reports her.
- **D136** (medium): the same through `reseat`, which carries her fresh seat
  out of her session and leaves no open seat and no repair.
- **D137** (medium for an app that versions seated rows, low for chess): the
  other seat's parentless row blocks a player's delete of her own row.
- **D138** (low): a row of the other seat hides a waiting move from its
  author's screen through the stored `_r_superseded` flag.

**The reproduction is untracked:** `tests/cold-review-3-probe.spec.ts`, 7
tests, 6 fail on purpose (the seventh is convergence, and passes). Run with
`npx playwright test --project=node tests/cold-review-3-probe.spec.ts --retries=0 --reporter=list`.
It is not committed; last time's ruling folded such a file into
`seat-attacks.spec.ts` as `test.fail`, and the same is presumably wanted, but
it is a ruling.

## Rulings wanted

1. **D135 to D137:** do the honest writers version within the row's own
   partition (session from the row being versioned; parents and a delete's
   columns only from that session and, in a seated table, that seat), with
   `_dai_open_seat`'s retire check scoped to the session (D136)? One fix
   covers all three.
2. **D138:** `_pending` computes supersession within the partition, as
   `_heads` does, instead of reading the stored flag?
3. **D134:** an entity id that commits to its session (format change), or
   apps keying every row by (session, entity)?
4. **The probe file:** fold it into `seat-attacks.spec.ts` as `test.fail`?

## Order remaining

1. Rulings on D134 to D138, then their fixes, red first. Step 5's seat model is
   not closed until D135 to D137 are, since each defeats a fix of this session
   with one legitimate row.
2. D127 (the arriving link's key outliving the arrival; high, not yet run).
3. Step 6 (it also closes the unsigned-row hole in every other table, and the
   whole-file load path, which verifies nothing: noted in D133).
4. The D124 route audit (its own session).
5. D32's bounded chase of the three rungs.

Still filed and not ordered: D139 (a WebKit retry), D120, D121, D123 items 2 and 3, D124, D128 to
D130, the four small items under D117.

## Traps met this session

- **A red for the wrong reason is not a red.** The first run of the attack
  tests through chess's `Store` failed on a named-parameter error and a NOT NULL
  column, not on the attack. Read why a `test.fail` failed before counting it.
- **A changed refusal registry has two more homes.** `ENTITY_OTHER_SESSION`
  failed `site-claims` until it was in `website/docs/host-bridge.md`'s table
  (which D133's commit had also left stale for `BATCH_UNSIGNED`), and
  `tests/refusal-wire.spec.ts` lists every code.
- **The model file's budget is a gate.** Rule text for D131 went 62 bytes
  over; it was tightened, not the budget raised. About 300 bytes remain.
- **The shell product.** Every runtime change leaves one new
  `apps/runner/public/hosts/<id>` from `npm run build`; a run before the last
  source edit leaves a stale one to drop.
- **WebKit `sealed-leave.spec.ts:25`** retried on two runs in a row (`734a4c3`,
  `0114b53`), about forty seconds each first time: filed as D139, not chased.
  Firefox `returning-document:511` retried again (D32).
- **`gh run list --commit` wants the full SHA.** With a short one it returns
  nothing, and a watcher looping on it waits forever in silence. Watch by run
  id, from `gh run list --branch`.
