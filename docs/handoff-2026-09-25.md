# Handoff, 25 September 2026: D126 ruled and fixed; CI green

Branch `identity/signed-authorship`, draft PR #5. The rulings in
`docs/handoff-2026-09-24.md` still bind; the standing rules are as there, with
**one step per session** (a step ends when its gate is green and its cold
review is read). One handoff per date, and it is always the latest: this file
replaces the D125/D122 handoff of the same date (`3adbde4`), which lives in git
history.

## Where things stand

- **Pushed:** through `9560321`. D126 is `f1f76e4` plus `9560321`, which keeps
  an anchor on one line (below).
- **Gate on `9560321`, the exact pushed tree:** push tier 1262 passed, 30
  skipped, 0 failed; count gate checked, chromium 762, node 499 (761 + the one
  new test). The whole WebKit project locally: 759 passed, 33 skipped, 1 failed,
  `sign-scope:27`, measured as pre-existing (D130 below).
- **CI on `9560321`**, run `36200300398`, read with `scripts/ci-verdict.mjs`
  and the names from the log: **success.** Chromium+node 1260, Firefox 751,
  WebKit 403 + 354, 0 failed, none flaky. Both D126 tests (`:1959`, `:1969`)
  passed on all three engines.
- **Closed by that verdict:** D126.
- **Read late:** run `36179699275` on `3adbde4` (the last handoff, docs only)
  was **red** on Firefox: `returning-document:511` failed both attempts at 3.0 m;
  `mailbox-link-e2e:1426` and `returning-document:381` flaky. `:511` is D32; it
  is now red in 2 of the last 3 runs on this branch.
- **Not read:** this handoff's own CI run.

## What this session did

**D126, ruled and built, red first on Chromium.** The ruling: a held copy is
never offered a merge from a different publisher, pinned or not; a different
publisher is a different document. `ingest` now compares the arriving
publisher with the held record's own `publicKeyFingerprint` (read fresh) for a
replicated document, **before the pin**. So a pinned copy and an unpinned one
are refused in one sentence (`strangersCopy`), through `refuseArrival`, with no
card. Before the fix, the pinned case showed the pin's impersonation sentence
and the unpinned case showed the merge card ("Open in my copy"). The later
sibling test takes the held record's key too, and keeps its refusal as a
backstop. The ruling is in `IDENTITY-ONE-LIVE-COPY`, with an anchor. The
unpinned test also checks that the stranger's key is not pinned. The one
failure in the first local run, `mailbox-link-e2e:1112` (the D119 miss), then
passed 12 of 12 alone.

**The anchor gate caught a rewrap.** The first push-tier run on `f1f76e4`
failed `rules.spec` "every anchor is still true": the new comment split "this
host keeps one copy per document" across two lines. Fixed in its own commit
and gated again.

**Cold read of the arrival refusals as a set** (a fresh agent, read only; 12
refusals, all through `refuseArrival`). Four filed; none run:

- **D127 (rated high):** `arrivedKey` and `arrivedSession` outlive the
  arrival; only `eject` clears them. A file picked after a link opened might be
  refused against the link's key, or have the link's key filed into it. First
  establish that a person can pick a file on that page at all.
- **D128:** "nothing on this device was changed" is said after writes
  (`rememberSessionKey`, `pinTrust`, `recordPublisher`, succession).
- **D129:** "This link" is said for files, and D126 before the pin replaces
  the pin's sharper wording; the logging of refusals is uneven.
- **On D126:** solo documents still have only the pin. A ruling is wanted on
  whether "a different publisher is a different document" reaches them.

The read found no comparison of a value with itself, and D126's refusal comes
before any write.

## Filed

- **D130:** `sign-scope:27` on WebKit, locally: the second document's frame
  never shows. 3 in 10 on `9560321`, 4 in 10 with `acd5753`'s `main.ts`.
  Pre-existing; not yet told apart from D32 or D123 item 3.
- D127, D128, D129 and the D126 solo question, above.

## Order remaining

1. **D80 green re-earned** on the new seat model (unchanged): in the D80 e2e,
   add the stronger forgery first (a binding by Bo to Ada's seat, backdated
   before hers, then the White move); include `_dai_confirm` in its seq scan;
   make `forgedPair` use `letIn`; fix its comment. Own commit.
2. Full chromium + node gate on the exact tree, push, CI per engine
   (`scripts/ci-verdict.mjs`, then the names from the log).
3. Second cold review over step 5 with `%TEMP%\review5s\attack.spec.ts` in range.
4. Step 6.
5. The D124 route audit (its own session).

Rulings wanted, none blocking D80: the D126 solo question; D129's wording
choice. D127 should be run before step 6 touches the arrival path.

Still filed and not ordered: D120, D121, D123 items 2 and 3, D124, D127 to
D130, the four small items under D117.

## Traps met this session

- **`grep -l "iOS reload" tests/` without `-r` lists nothing** ("Is a
  directory"), and Playwright given no files runs the whole project. It ran all
  of WebKit, a superset, so nothing was missed, but the command in the notes
  needs `-r`.
- **A comment rewrap can break a rule anchor.** `rules.spec` reads anchors as
  exact substrings; the narrow local run did not include it, the push tier did.
- **A monitor matching "failed" matches test names** (`cli.spec` "names what
  failed"). Match the summary line, `^\s+N failed`, or the exit marker.
- **A docs-only commit's CI run is still a verdict.** `3adbde4`'s run was red
  and unread until this session looked.
