# Handoff, 25 September 2026: identity sitting, step 5, mid step 3 of its order

Branch `identity/signed-authorship`, draft PR #5. Start by reading
`docs/handoff-2026-09-24.md` (the rulings, still binding), then this. Standing
rules as there, plus one given 25 September: **one step per session.** A step
ends when its gate is green and its cold review is read; that is the handoff
point, whatever context remains.

## Where things stand

- **Pushed:** `7a35fb5` (finding 5 red), with `2eb86af` and `02d5784`. CI run
  `36075418998` on it: chromium+node 1238 passed, 3 failed (exactly the three
  red claims), Firefox 746 and both WebKit shards green; `checks` red on a stale
  impact map (fixed in `873fadd`).
- **Local, not pushed:**
  - `873fadd` identity: a parent outside the row's entity hides nothing
    (finding 5, T1-D35). All six supersession sites plus D4's export closure;
    each reverted alone and caught. Two new merge vectors; Python and Rust 20 of
    20. Products whole (host `7dba1413b762b09b`). Commit tier: 1243 passed, the
    only reds the two HEAD-reading product guards, green once committed.
  - `2e3d227` identity red: the five attacks as named tests
    (`tests/seat-attacks.spec.ts`), each red on the old model at its claim.
- **Uncommitted in the tree: the whole seat-model rebuild (order step 3),**
  products regenerated (host `186d3c3df9706a15`; three intermediate hosts from
  this pass were deleted as never shipped). Commit tier on this tree: 1252
  passed, 27 skipped, **4 failed**:
  - `host-retention` and `staged-runtime`: read HEAD; go green on commit.
  - `site-claims:20`: `CANNOT_CONFIRM` is not in the website's refusal table
    (the Host Bridge Protocol page). Add it there.
  - `invite-identity:252` (test 6, a new device is a new author): expects the
    name dialog at once. The fixture now asks a name only once seated
    (`needsName` in `tests/fixture/chess/store.js`), so the test must first let
    the new device in (pull the creator, then the device), as `letIn` does in
    `tests/mailbox-link-e2e.spec.ts`. Decide which the test claims.
  Fix those two, rerun the tier, then commit step 3 whole.

## What step 3 built (in the tree)

- `src/session-id.ts`: `sessionIdTools()`, one self-contained sync SHA-256 and
  `sessionIdOf`; node imports the result, and `bridgeScript` passes its source
  into the frame beside the names (bridgeMain is serialized and cannot import).
  SQL function `dai_session_id`, registered on every runtime connection
  (`newDatabase` in the bootloader) and in tests via `tests/session-db.ts`.
- Views (`src/replicated.ts`): `_dai_creator` (nonce row whose hash with its
  author is its session), `_dai_open_seat` (creator's own current versions),
  `_dai_holder` (creator's seat; else the creator's earliest confirmation by
  seq), `_dai_member`; seated admission is `holds()`, no clock; `t_unseated`
  reports only no-seat or held-by-another; new `t_pending` (rows by an author
  waiting on a confirmation; the same on every copy). Tables: `_dai_seat.nonce`,
  new `_dai_confirm(seat, holder)`. `verifiedRank`/`verifiedBefore` removed:
  unsigned forgery under another id is step 6's `BATCH_UNSIGNED`, for every
  table alike.
- Writers: `startSession`, `confirmSeat` (`src/replicated-rows.ts`); runtime
  `session.create` (nonce, no self-binding), `session.confirm` (NOT_SEAT_CREATOR,
  CANNOT_CONFIRM), `reseat` only on an unheld seat asked for twice; `seatGate`
  lets a waiting copy write for the seat it asked for.
- Kit: `pendingSeat`, `confirmSeats` (creator's copy, capturing `dai:merged`
  listener and at load), `claimSeat` returns null while waiting.
- Rules: IDENTITY-FIRST-SIGNER withdrawn into a `WITHDRAWN` record with its
  attack; IDENTITY-SEAT-CONFIRMED; session rules, VIEWS, SURFACE, refusals
  rewritten; model budget 141,386. Docs: identity.md seat rows, rule 5, 24 Sept
  rulings, Loss sentence (finding 7); backlog D114 closed, D115 (finding 8),
  D116 (finding 10); T1-D29 note replaced.
- Every view clause reverted alone and caught (one needed a new test: a
  member's version of the open seat's row).
- Apps: chess fixture lets a waiting player move and shows its own rows from
  `t_pending`; tic-tac-toe and request got the least that keeps them working
  (new seat reads, waiting state, seating their one asker; named `confirm`
  exceptions in `scripts/check-seats.mjs`). E2Es: `letIn` helper; the contested
  e2e asserts one outcome (both asks reach the creator in one merge via C's
  shared copy, nobody seated, both told, repair seats B, B's e5 never stands).

**A consequence to confirm with the ruling-giver:** over the mailbox the host
applies one batch per merge event, so the creator's copy seats the first ask it
reads; a contest needs two asks in one merge (a file, or a copy shared by link).
That is the ruling read literally, and it is how the e2e is built.

A drafted commit message for step 3 was in this pass's scratchpad and is gone
with it; the list above is its substance.

## Order remaining (unchanged)

3. (finish) the two failures, tier green, commit step 3 whole.
4. D80 green re-earned: in the D80 e2e (`mailbox-link-e2e`, "D80: a forged
   copy's move"), add the review's stronger forgery first: a binding by Bo to
   Ada's seat, backdated before hers, then the White move; include
   `_dai_confirm` in its seq scan; make `forgedPair` use `letIn`; fix its
   comment ("held that seat when it was written"). Own commit.
5. Full chromium + node gate on the exact tree, push, CI per engine
   (`scripts/ci-verdict.mjs`).
6. Second cold review over step 5 with `%TEMP%\review5s\attack.spec.ts` in range.
7. Then step 6.

## Traps met this pass

- **bridgeMain cannot import.** A new import used inside it minifies to a bare
  name (`jt is not defined`) and only a browser run shows it. Pass source in.
- **A wait that passes vacuously.** `letIn` first counted "asks still waiting"
  and passed with zero asks, reading the wrong frame (`appFrame` takes the first
  grandchild, stale after a remount). It now states what it looked at (author
  id, asks > 0) and reads through `app(page)`.
- **A setup instruction not seen to take.** Blocking the creator's relay reads
  with a context route: count the aborts and assert them.
- **Wait for the state the next step needs:** both asks in the mailbox, not
  "B opened, C opened".
- **Templated SQL evades the lint.** `FROM ${table}` escaped `shared-base-read`;
  that pass was for an unrelated reason. Fixed by `t_pending`, not by the lint.
- **Intermediate retained hosts.** Every `npm run build` keeps a host; delete
  the ones that never shipped before committing.
- **Do not edit specs while a tier runs**: workers import them as they go.
- `refusal-wire` and the website refusal table both list codes; a new code
  goes in both, and in `src/refusals.ts` and `APP_REFUSALS`.
