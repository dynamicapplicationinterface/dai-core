{
  "version": 1,
  "note": "A starter set. A published run wants several hundred; these exist so the pipeline is real and the scoring is settled before anybody spends money on completions.",
  "contract": "Every prompt asks for the same three ids, because an application nobody can drive cannot be scored. Requiring them is part of the task rather than a concession: an application whose controls have stable names is easier for a person to automate too.",
  "prompts": [
    {
      "id": "reading-list",
      "ask": "a reading list: add a book with a title and an author, mark one as finished, and see how many are left",
      "requires": {
        "#what": "text input for the title",
        "#add": "button that adds it",
        "#save": "button that saves into the file"
      },
      "try": [
        { "fill": "#what", "text": "The Peregrine" },
        { "click": "#add" },
        { "wait": 500 },
        { "click": "#save" }
      ],
      "expect": {
        "sql": "SELECT count(*) AS n FROM sqlite_schema WHERE type = 'table' AND name NOT LIKE 'sqlite_%' AND name <> '_dai_meta'",
        "rows": [{ "n": 1 }],
        "why": "One table of its own. The count is deliberately about shape rather than contents: a model may reasonably call the column title or name, and refusing it for that would be scoring vocabulary."
      }
    },
    {
      "id": "habit-tracker",
      "ask": "a habit tracker: add a habit, tick it off for today, and see a streak",
      "requires": {
        "#what": "text input for the habit",
        "#add": "button that adds it",
        "#save": "button that saves into the file"
      },
      "try": [
        { "fill": "#what", "text": "Walk" },
        { "click": "#add" },
        { "wait": 500 },
        { "click": "#save" }
      ],
      "expect": {
        "sql": "SELECT count(*) AS n FROM sqlite_schema WHERE type = 'table' AND name NOT LIKE 'sqlite_%' AND name <> '_dai_meta'",
        "atLeast": 1,
        "why": "A habit tracker may reasonably use two tables — habits and the days they were done — so this asks for at least one rather than exactly one."
      }
    },
    {
      "id": "invoice-log",
      "ask": "a log of invoices: record who it was for, how much, and whether it has been paid; show the unpaid total",
      "requires": {
        "#what": "text input for the client",
        "#add": "button that adds it",
        "#save": "button that saves into the file"
      },
      "try": [
        { "fill": "#what", "text": "Acme" },
        { "click": "#add" },
        { "wait": 500 },
        { "click": "#save" }
      ],
      "expect": {
        "sql": "SELECT count(*) AS n FROM sqlite_schema WHERE type = 'table' AND name NOT LIKE 'sqlite_%' AND name <> '_dai_meta'",
        "atLeast": 1
      }
    },
    {
      "id": "packing-list",
      "ask": "a packing list for a trip: add items, tick them off, and see what is left",
      "requires": {
        "#what": "text input for the item",
        "#add": "button that adds it",
        "#save": "button that saves into the file"
      },
      "try": [
        { "fill": "#what", "text": "Passport" },
        { "click": "#add" },
        { "wait": 500 },
        { "click": "#save" }
      ],
      "expect": {
        "sql": "SELECT count(*) AS n FROM sqlite_schema WHERE type = 'table' AND name NOT LIKE 'sqlite_%' AND name <> '_dai_meta'",
        "atLeast": 1
      }
    },
    {
      "id": "two-player-game",
      "shape": "session",
      "ask": "Connect Four for two people who play by sending the document back and forth, each on their own phone: start a game against someone, invite them, drop a disc in a column when it is your turn, and see whose turn it is and who has won. Only the two people in a game may play it.",
      "note": "Scored as far as a harness without a host can see: a shared document writes only under a host, and this harness opens the file without one. So it stops at shaped — the lint's shared-table checks at checked, the build, then the declared shape and no stored derived state — and mounted and usable are reported as not measured, never as passed. The same application driven across two devices in the real host is what tests/examples-shared.spec.ts does for the examples.",
      "expect": {
        "derived": ["board", "grid", "cells", "winner", "result", "status", "current_player", "next_player", "to_move", "whose_turn", "updated_at", "last_move", "move_count", "moves_count"],
        "why": "The session constraints, where they can be read from the source: a -- dai:profile session line, at least one shared table, and no shared column holding what should be derived from the moves. Reading _current, listening for dai:merged, showing conflicts and writing through window.dai.replicated are the lint's, at checked."
      }
    }
  ]
}
